# ReactRouter
Demo application using React router

Running the app:
 - npm i
 - open src/index.html

debug mode:
`webpack`

watch mode:
`webpack --watch`

production mode:
`NODE_ENV=production webpack`
